## User

What are the areas of improvements?
Features to add?
Code improvements?
Architecture improvements?
Tools suggestions?
Libraries suggestions?

---

## Assistant (Plan · DeepSeek V4 Flash Free (New) · 174.4s)

_Thinking:_

The user is asking for a comprehensive analysis of areas of improvement for the project. This is a research/analysis question in plan mode. I should explore the codebase to give an informed answer, then present areas of improvement across the categories they listed:
1. Features to add
2. Code improvements
3. Architecture improvements
4. Tools suggestions
5. Libraries suggestions

Let me explore the codebase first to give an informed, grounded response. Let me use explore agents to understand the current state of the frontend and backend.

Let me look at the project structure, key files, and understand what's already built. I'll launch parallel explore agents.

Actually, since I've been working in this codebase extensively, I already know a lot:
- Frontend: Vite + React 19 + TanStack Router, Tailwind, shadcn/ui (Base UI), dnd-kit, better-auth/react
- Backend: Express 5 + Bun, Drizzle ORM (PostgreSQL), better-auth, Zod env validation
- Features: notes CRUD, labels, pinning, archiving, trash, colors, images (S3), checklists/task lists, search, drag-and-drop reordering, list/grid layouts, auth (email/password + GitHub/Google)

But let me do some exploration to ground my recommendations in the actual current state — check for things like: missing tests, TODOs, monorepo setup, CI/CD, error handling, pagination, etc.

Let me launch explore agents in parallel to gather details.

Let me explore the current state of the codebase thoroughly before making recommendations.

**Tool: task**

**Input:**
```json
{
  "description": "Explore frontend architecture",
  "prompt": "Explore the frontend app at /home/hussein/projects/google-keep-clone/apps/frontend (Vite + React 19 + TanStack Router + TypeScript). Be thorough (medium depth).\n\nReport on:\n1. Project structure: key directories under src/, what features exist (features/notes, features/auth, etc.), the routing setup (routeTree.gen.ts, routes), and how pages are organized.\n2. State management approach: how is server state handled? (React Query? TanStack Query? plain fetch/useEffect?). Look for data fetching patterns, caching, mutations, optimistic updates.\n3. UI component library: what's under src/components/ui (shadcn-style components, which base lib — Radix or Base UI), styling approach (Tailwind version, CSS vars, theme).\n4. Testing: what test files exist, what framework (vitest/playwright), coverage of what areas.\n5. Code quality: any TODO/FIXME/HACK comments, large files, duplicated logic.\n6. Any obvious gaps: error handling patterns (toasts?), loading states, skeletons, optimistic UI, accessibility patterns.\n\nReport concrete file paths and line numbers for key findings. Be concise but specific.",
  "subagent_type": "explore"
}
```

**Output:**
```
<task id="ses_02ac4b543ffeCEqOc1SwWvFXZG" state="completed">
<task_result>
I have completed a thorough exploration. Here is the report.

# Frontend Exploration Report: `/home/hussein/projects/google-keep-clone/apps/frontend`

## 1. Project Structure & Routing

**Layout** (`src/`):
- `src/routes/` — file-based routes: `__root.tsx` (root shell), `index.tsx` (landing), `notes.tsx` (notes, protected)
- `src/routeTree.gen.ts` — generated by `bun run generate-routes` (`tsr generate`); declares routes `/` and `/notes` (lines 26–46), plus TanStack Start SSR registration (lines 79–86)
- `src/router.tsx` — `createTanStackRouter` with `scrollRestoration: true`, `defaultPreload: "intent"`
- `src/features/` — feature-sliced: `notes/`, `labels/`, `landing/`. Each has `pages/`, `components/`, `api.ts`, `types.ts`
- `src/components/ui/` — shadcn-style primitives (18 files)
- `src/components/theme/` — dark-mode provider + toggle
- `src/lib/` — `api.ts` (axios), `auth-client.ts` (better-auth), `env.ts` (Zod), `utils.ts` (`cn`)
- `src/hooks/use-mobile.ts`, `src/tests/setup.ts`, `src/styles.css`

**Routing setup:** Routes are thin wrappers. `routes/notes.tsx` wraps `NotesPage` in `<ProtectedRoute>`; `routes/index.tsx` renders `LandingPage`. The app uses **TanStack Start** (`@tanstack/react-start` plugin in `vite.config.ts:11`); `__root.tsx` uses `createRootRoute` + `shellComponent`, `HeadContent`, `Scripts`, and injects `<Toaster />`, `TooltipProvider`, `ThemeProvider`, and devtools (lines 64–94). Note: no `index.html` or manual entry file exists in the app root — the Start plugin generates the entry.

## 2. State Management

**There is NO TanStack Query / React Query** — a grep for `useQuery|useMutation|QueryClient|@tanstack/react-query` returns nothing. Server state is managed with **plain `useState` + `useEffect` + direct axios calls**:

- **Data fetching:** `src/features/notes/pages/notes-page.tsx` holds all notes state locally: `loadNotes()` (lines 34–48) and `loadLabels()` (50–57) in `useEffect`s (lines 59–77). Search is debounced 250ms via `setTimeout` ref (lines 65–77).
- **API layer:** thin async wrappers over a shared axios instance (`src/lib/api.ts`, `withCredentials: true`) in `src/features/notes/api.ts` (get/create/update/trash/restore/delete/image-upload/reorder, lines 4–93) and `src/features/labels/api.ts`.
- **Mutations:** every mutation re-fetches the full list afterwards (`handleCreate`/`handleUpdate`/`handleDelete`/`handleTogglePin`/`handleArchive`/`handleTrash`/`handleRestore`/`handlePermanentDelete`, `notes-page.tsx:79–139`). No caching, deduping, or background refetch.
- **Optimistic updates:** only for drag-and-drop reorder — `handleReorderLive` mutates local state mid-drag (`notes-page.tsx:141–151`), persisted by `handleReorder` with rollback `.catch(() => loadNotes())` (line 155). No optimistic pin/archive/trash with rollback.
- **Autosave:** `take-note-input.tsx` debounces saves 600ms (`SAVE_DEBOUNCE_MS`, lines 40, 113–143); new notes are created on first save, then updated (lines 145–178); empty notes are auto-deleted on close (lines 190–224).
- **Auth state:** `authClient.useSession()` hook (`notes-page.tsx:23`) and manual `authClient.getSession()` in `protected-route.tsx:10` and `landing-page.tsx:15`.

## 3. UI Component Library & Styling

- **Base lib = Base UI (not Radix):** `@base-ui/react` ^1.6.0. Used in `button`, `dialog`, `sheet`, `alert-dialog` (all wrap `Dialog`), `dropdown-menu` (wraps `Menu`), `tooltip`, `avatar`, `input`, `scroll-area`, `separator`, plus `mergeProps`/`useRender` in `sidebar.tsx:3–4` and `badge.tsx:1–2`. `components.json` style is `"base-nova"` with `baseColor: neutral`, `cssVariables: true`, lucide icons.
- **Styling = Tailwind CSS v4** (`tailwindcss` ^4.1.18 via `@tailwindcss/vite`), CSS-first config: `src/styles.css` does `@import "tailwindcss"`, `tw-animate-css`, `shadcn/tailwind.css`, Inter font (lines 1–4), with `@custom-variant dark` (line 6) and `@plugin "@tailwindcss/typography"`.
- **Theme:** CSS custom properties in **oklch** for `:root` and `.dark` (styles.css:69–136), incl. full sidebar and chart token sets. Dark mode toggled by class + `colorScheme` via `src/components/theme/provider.tsx` (FOUC-prevention inline script, lines 17–22). CVA variants + `cn()` (clsx + tailwind-merge) everywhere.

## 4. Testing

- **Unit (Vitest + Testing Library, jsdom):** 4 test files under `src/features/notes/components/__tests__/`: `note-card.test.tsx` (13 tests), `notes-grid.test.tsx` (4), `note-form.test.tsx` (10), `edit-note-dialog.test.tsx` (6). Configured in `vitest.config.ts` (`src/**/*.test.{ts,tsx}`); `src/tests/setup.ts` runs `cleanup()` after each. Heavy `vi.mock` of UI deps and lucide icons. Coverage is **notes UI components only** — no tests for api functions, pages, hooks, auth, labels, or landing.
- **E2E (Playwright, chromium only):** `e2e/` — `auth.spec.ts` (3 tests), `landing.spec.ts` (2), `notes.spec.ts` (4: create, search, archive/restore, trash/restore), plus `helpers.ts` (signUp/signIn/createNote/noteCard/openView/signOut). `playwright.config.ts` auto-spawns backend + frontend via `webServer` (root `bun run dev:backend` / `dev:frontend`, lines 35–50) and hardcodes `/usr/bin/chromium`.

## 5. Code Quality

- **No TODO/FIXME/HACK/XXX/@ts-ignore** anywhere in `src/` (grep returned nothing).
- **Largest files:** `src/components/ui/sidebar.tsx` (722 lines, standard shadcn sidebar), `src/features/notes/components/take-note-input.tsx` (544), `note-form.tsx` (458), `dropdown-menu.tsx` (269), `notes-grid.tsx` (259), `note-card.tsx` (255), `field.tsx` (235).
- **Duplicated logic:**
  - `take-note-input.tsx` and `note-form.tsx` duplicate ~200 lines: identical `NOTE_COLORS` arrays (take-note-input.tsx:28–38 / note-form.tsx:29–39), same Zod schema, same color-picker + label-picker + image-upload (handleFileSelect/handleRemoveImage) + checklist-toggle handlers.
  - Checklist progress bar duplicated 3×: `note-card.tsx:129–150`, `checklist-editor.tsx:102–116`, and the exported-but-unused `ChecklistProgress` (`checklist-editor.tsx:121–138`).
  - Near-identical fetch-after-mutate handlers in `notes-page.tsx:110–139`.
  - Spinner markup repeated in `notes-page.tsx:190–193`, `protected-route.tsx:20–24`, `landing-page.tsx:24–30`.
- **Smells:** `checklist-utils.ts:1` imports `createChecklistItem` from a component file (layer inversion); two `biome-ignore` for `useExhaustiveDependencies` (`notes-page.tsx:59,64`); `biome.json` disables a11y/suspicious rules for `ui/` and tests.

## 6. Obvious Gaps

- **Error handling is minimal:** `toast.error` only on image upload/delete and save failures (`note-form.tsx:138,157`; `take-note-input.tsx:176,219,240,252`). The page-level loaders swallow all errors silently (`notes-page.tsx:43–44` `catch { setNotes([]) }`, `54–55`). No axios interceptors, no error boundary, no error state surfaced to the user for note CRUD failures; `label-picker.tsx:34` swallows create errors, and OAuth handlers in `auth-form.tsx:71–77` have no try/catch.
- **Loading states:** only spinners; `Skeleton` exists (`src/components/ui/skeleton.tsx`) but is used solely by `SidebarMenuSkeleton` (`sidebar.tsx:599–624`) — the notes grid loading state is a spinner instead of skeletons.
- **Optimistic UI:** only drag reorder (see §2). All other actions block on refetch.
- **Accessibility:**
  - `note-card.tsx:48–62` uses a `div role="button"` (biome-ignored); Enter opens but **Space is not handled**; icon buttons rely on `title` with no `aria-label`.
  - The delete-confirmation overlay in `edit-note-dialog.tsx:98–133` is hand-rolled: `role="dialog"` with no focus trap, no `aria-labelledby`, no `aria-describedby`, and Escape only works when focus is inside.
  - Base UI Dialog (main editor) handles focus trapping/announcements properly.
- **Other:** no empty-state distinction for search-with-no-results (message is per-view only, `notes-grid.tsx:41–54`); image deletion in `note-form` isn't optimistic (awaits server first); `auth-form` server errors are rendered inline (fine) but only for email/password, not OAuth.
</task_result>
</task>
```

**Tool: task**

**Input:**
```json
{
  "description": "Explore backend architecture",
  "prompt": "Explore the backend app at /home/hussein/projects/google-keep-clone/apps/backend (Express 5 + Bun + Drizzle ORM + PostgreSQL + better-auth). Be thorough (medium depth).\n\nReport on:\n1. Structure: src/ layout, how features are organized (features/notes, features/auth, etc.), routers/controllers/services pattern.\n2. Database: schema files under src/db/schema, migrations setup, how queries are written (raw SQL vs query builder), transactions usage.\n3. Auth: better-auth setup, middleware, route protection, session handling.\n4. Error handling: global error middleware, error responses shape, validation (Zod? where?).\n5. Testing: test files, framework, what's covered.\n6. Code quality: TODO/FIXME, large files, repeated patterns.\n7. Infrastructure gaps: rate limiting, pagination, logging, observability, caching, file uploads handling (S3).\n\nReport concrete file paths and line numbers for key findings. Be concise but specific.",
  "subagent_type": "explore"
}
```

**Output:**
```
<task id="ses_02ac4ad23ffeeh1MW2FKuifM82" state="completed">
<task_result>
# Backend Exploration Report

## 1. Structure

**Layout** (`src/`):
```
src/
├── index.ts                      # Bootstrap (Bun runtime)
├── app.ts                        # Express app assembly
├── lib/                          # env.ts, auth.ts, storage.ts
├── db/                           # index.ts, types.ts, seed.ts, schema/, migrations/
├── features/
│   ├── auth/middleware.ts        # requireAuth
│   ├── notes/  {router, controller, service, middleware}.ts
│   └── labels/ {router, controller, service}.ts
└── tests/                        # app.test.ts, helpers.ts, setup.ts
```

**Pattern**: Classic router → controller → service layering, one feature folder per domain.
- Routers are thin: `src/features/notes/router.ts` (17 lines) just maps HTTP verbs to controller functions; `src/features/labels/router.ts` (11 lines).
- Controllers (`notes/controller.ts`, `labels/controller.ts`) handle `req`/`res`, read `req.body`/`req.query`, and do per-handler try/catch.
- Services (`notes/service.ts`, `labels/service.ts`) own all Drizzle query logic.
- Feature-specific middleware only exists in notes (`src/features/notes/middleware.ts` — multer upload config).

**App wiring** (`src/app.ts`): global middleware (morgan, json, cookie-parser, cors) → `/api/auth/me` + `/api/health` → better-auth handler at `app.all("/api/auth/{*any}", toNodeHandler(auth))` (line 50) → global `requireAuth` (line 51) → feature routers (lines 52–53).

## 2. Database

- **Driver/ORM**: Drizzle ORM with `node-postgres`. DB singleton at `src/db/index.ts:5` — `drizzle(env.DATABASE_URL, { schema })`. `DrizzleDB` type in `src/db/types.ts`.
- **Schema** (`src/db/schema/`): `auth.ts` (better-auth `user`, `session`, `account`, `verification`), `notes.ts`, `labels.ts` (+ join table `note_labels` with composite PK), `note-images.ts`, `relations.ts`, shared `timestamps.ts` helper (`withTimestamps`). All FK to `user.id` with `onDelete: "cascade"`; all queries user-scoped.
- **Migrations**: 9 drizzle-kit migrations `0000–0008` in `src/db/migrations/` (auth tables + notes → labels → note_labels PK fix → pins/colors/archive/trash/sort_order → note_images → checklist). Configured in `drizzle.config.ts` (output `./src/db/migrations`, dialect postgresql). Scripts: `db:generate`, `db:migrate`, `db:push`, `db:studio`, `db:seed`.
- **Query style**: 100% Drizzle query builder. Only one raw SQL fragment: search on checklist JSONB in `notes/service.ts:32` — `` sql`(${ilike(...)} OR ${notesTable.checklist}::text ilike ...)` ``.
- **Transactions**: `db.transaction` used in `notes/service.ts:91` (createNote), `:118` (updateNote), `:208` (reorderNotes), and `labels/service.ts:58` (setNoteLabels). Note: `softDeleteNote`, `restoreNote`, `permanentDeleteNote` are **not** transactional (labels fetched after, no rollback risk).
- **Gaps**: no indexes on `notes` beyond PK (no `user_id` index; FK cascade delete relies on it); no pagination on `getNotes` (`notes/service.ts:15-52` returns all rows).

## 3. Auth

- **better-auth** configured in `src/lib/auth.ts`: `drizzleAdapter`, email/password enabled, GitHub + Google OAuth (providers referenced with non-null `!` even though env vars are optional — `env.ts:11-14`).
- **Middleware**: `src/features/auth/middleware.ts:4-18` — `requireAuth` calls `auth.api.getSession({ headers: fromNodeHeaders(req.headers) })` on **every request** (no caching), 401s if absent, then mutates `req.session`/`req.user`.
- **Protection model**: applied globally in `app.ts:51` *after* all auth routes, so `/api/notes` and `/api/labels` are fully protected. `/api/auth/me` (app.ts:36-44) and `/api/health` sit before it.
- **Session**: cookie-based; CORS with `credentials: true` (app.ts:21-26). `req.user.id` read via `(req as any).user.id` everywhere — no Express `Request` type augmentation for `user`/`session`.
- **Seed** (`src/db/seed.ts:407`) signs users up via `auth.api.signUpEmail` to get correct password hashing.

## 4. Error Handling

- **No global error middleware.** `src/app.ts` has no `ErrorRequestHandler`. Express 5 forwards async errors automatically, so unexpected errors fall through to Express's default HTML error handler — responses are not JSON-shaped for unhandled errors.
- **Per-controller try/catch** with ad-hoc JSON responses, and the shape is inconsistent:
  - `{ error: "..." }` (most handlers)
  - `{ message: "..." }` (`features/auth/middleware.ts:10`, labels `createLabel`/`updateLabel` name-required checks)
  - **`res.status(500).json({ error })` in `notes/controller.ts:35` leaks the raw error object/stack** — the only place this happens.
- **Validation**: Zod is used **only** for env validation (`src/lib/env.ts:3-21`, `validateEnv` throws on startup). **No request body/query schema validation** anywhere — controllers manually check fields (e.g., `if (!name)`, `normalizeChecklist` at `notes/controller.ts:6-20`, `reorderNotes` array check at `:215`).
- Multer-specific errors handled in `uploadNoteImage` (`notes/controller.ts:176-184`: `LIMIT_FILE_SIZE`, invalid mime).

## 5. Testing

- **Framework**: Vitest + Supertest (`vitest.config.ts`). Coverage thresholds at 80% (lines/functions/branches/statements). Config injects a local Postgres `DATABASE_URL` and sets `setupFiles: ./src/tests/setup.ts`.
- **Files**:
  - `src/tests/app.test.ts` (391 lines) — health check; notes CRUD incl. checklist create/update, search (title/content/checklist), trash/restore/permanent delete, archive & trash filters, label filters, "reject updating trashed notes", label CRUD, note↔label associations.
  - `src/tests/helpers.ts` — `createTestApp()` that **bypasses real auth** by injecting a stub middleware setting `req.user`/`req.session` to `TEST_USER_ID` (line 30-34).
  - `src/tests/setup.ts` — ensures the test user row exists and cleans test data before/after.
- **Coverage gaps**: tests hit the real DB (no mocks/containers), no tests for image upload (S3/multer), auth endpoints, `requireAuth` 401 path, or 500 error paths.

## 6. Code Quality

- **No TODO/FIXME/HACK markers** found anywhere in `src/`.
- **Large files**: `src/db/seed.ts` (553 lines, mostly data arrays — ~500 lines of literal content), `src/tests/app.test.ts` (391), `src/features/notes/service.ts` (354), `notes/controller.ts` (223). The rest are small.
- **Repeated patterns**:
  - Presigned-URL image mapping duplicated 3× (`notes/service.ts:63-82`, `:135-154`, `:326-351`) — extractable into one helper.
  - `setNoteLabelsTx` (`notes/service.ts:220-244`) duplicates `setNoteLabels` (`labels/service.ts:53-78`).
  - `(req as any).user.id` repeated ~13× across controllers (no typed `Request` augmentation).
  - `requireAuth` params typed `any`; several `tx: any` params in service.
  - Dead/commented boilerplate in `app.ts:28-34` (ejs/static).
- **TypeScript**: strict mode on, `verbatimModuleSyntax`, `noUncheckedIndexedAccess` (tsconfig). The `any` casts noted above are the main strictness leaks.
- **Bug-prone**: in `uploadNoteImage` (`notes/controller.ts:162-164`) the S3 upload happens **before** the DB insert — a DB failure orphans the S3 object.

## 7. Infrastructure Gaps

| Area | Status |
|---|---|
| **Rate limiting** | None — no `express-rate-limit` or equivalent anywhere. |
| **Pagination** | None — `getNotes` returns all notes (no `limit`/`offset`/cursor in service or router). |
| **Logging** | Only `morgan("dev")` (app.ts:15). No structured logger (pino/winston), no request IDs. |
| **Observability** | None — no tracing, metrics, Sentry/OpenTelemetry. `index.ts` has basic startup error handling and graceful shutdown (lines 9-16). |
| **Caching** | None — DB is hit directly on every request incl. per-request session lookup in `requireAuth`. |
| **File uploads (S3)** | **Handled**: `src/lib/storage.ts` — S3Client, presigned GET URLs (1h expiry), lazy singleton via `getStorage()` (returns `null` if S3 not configured → graceful degradation), key generation `userId/noteId/uuid.ext`. Multer memory storage, 10MB limit, allowlist (JPEG/PNG/GIF/WebP) in `notes/middleware.ts`. |
| **Upload gaps** | `width`/`height` always null (no image dimension parsing); orphaned S3 objects on DB-insert failure; S3 deletes are best-effort (`Promise.allSettled` in `permanentDeleteNote`); `deleteNoteImage` silently skips storage deletion when S3 unconfigured. |
| **Security hardening** | No `helmet`; `x-powered-by`/`etag` disabled (app.ts:19). |
| **API docs** | Bruno collection scaffold at `bruno/Google Keep Clone/bruno.json` (no request files yet). |
| **Config** | Env validated via Zod at startup; S3 vars optional (`.env.example` documents all, incl. local garage endpoint). |
</task_result>
</task>
```

---

## Assistant (Plan · DeepSeek V4 Flash Free (New) · 23.6s)

_Thinking:_

I have a very thorough understanding now. Let me compile a comprehensive but well-organized set of recommendations across the categories the user asked about:

1. Features to add
2. Code improvements
3. Architecture improvements
4. Tools suggestions
5. Libraries suggestions

I'm in plan mode, so I should present a plan/recommendations. The user asked an open-ended "what are the areas of improvements" — this is more of an advisory/research question than a concrete implementation task. I should present findings in a structured way and perhaps ask what they'd like to prioritize.

Let me organize the recommendations by the categories, grounded in the exploration findings with file references. I should be comprehensive but concise.

Let me think about the most impactful items:

### Features to add
Google Keep parity features:
- Reminders (with notifications)
- Collaboration/sharing notes with other users
- Note colors palette (already have basic colors) — full color picker
- Pin to top (already there)
- Search within note body + labels filter UI (search exists)
- Dark mode (exists)
- Duplicate note
- Copy to Google Docs-like export (markdown export)
- Print
- Trash empty / empty trash button (permanent delete exists but maybe bulk)
- Bulk selection/multi-select
- Archive from card
- Note version history
- Drawings (Keep has drawing)
- Voice memo/transcribe
- Labels management screen (exists partially — label-picker)
- Custom layouts (exists grid/list)
- Web Push notifications for reminders
- Offline PWA support
- Keyboard shortcuts
- Note templates
- Checkboxes (done)
- Zoom/font size settings

High-value given the codebase:
- Reminders + notifications
- Collaboration (multi-user) — big backend change
- Markdown/export
- Duplicate note
- Empty trash
- Search highlighting, search by label
- Pagination/infinite scroll (needed given 100 notes seed)
- Better empty states (search no results)
- Keyboard shortcuts
- PWA/offline

### Code improvements
Frontend:
- Deduplicate take-note-input.tsx and note-form.tsx (~200 lines duplicated: NOTE_COLORS, Zod schema, color/label/image/checklist handlers) — extract shared hooks/components
- Extract ChecklistProgress into one component (duplicated 3x)
- Add TanStack Query for server state (big win) — replaces fetch-after-mutate pattern
- Error handling: page loaders swallow errors; add error states + toasts; axios interceptors
- Skeleton loading states instead of spinners
- Accessibility: note-card Space key, aria-labels, fix hand-rolled delete dialog focus trap
- Fix layer inversion: checklist-utils imports from component
- Optimistic updates for pin/archive/trash
- Split large files (take-note-input 544 lines)

Backend:
- Add global Express error middleware with consistent JSON shape
- Fix `res.status(500).json({ error })` leaking error object
- Add Zod request validation (shared schemas between FE/BE)
- Add user_id index on notes + note_labels
- Make softDelete/restore/permanentDelete transactional
- Fix S3 orphaned object bug (upload before DB insert)
- Extract presigned-URL mapping helper (duplicated 3x)
- Type augmentation for req.user/req.session (remove 13x `as any`)
- Add pagination to getNotes
- Rate limiting (esp. auth)
- helmet, structured logging
- Cache session lookups

### Architecture improvements
- Move to TanStack Query (server state) — the biggest architecture win
- Monorepo shared packages: shared validation schemas (Zod), shared types between FE/BE
- API versioning / OpenAPI specs (openapi-typescript for typed client generation)
- Domain events / notifications service for reminders
- Repository/service pattern already good; extract storage abstraction (already has getStorage)
- Backend: use better-auth server plugins for session caching
- Add a proper error-code taxonomy
- Consider edge: streaming, websockets for collaboration (out of scope probably)
- Feature-sliced design already good on FE

### Tools suggestions
- Turborepo for the monorepo (currently likely bun workspaces + concurrent scripts)
- Changesets for versioning (probably overkill for this)
- Storybook for component development
- Playwright already there — add more e2e
- Vitest coverage already there — add Istanbul plugin
- drizzle-kit studio already
- Add ESLint/TypeScript config — already has Biome; maybe add oxlint?
- Better test coverage reporting (vitest --coverage)
- Hurl or Bruno (exists) for API testing
- Sentry for error monitoring
- GitHub Actions CI (is there CI? Let me check — I didn't see. Probably worth suggesting CI)
- Deps: rennovate/dependabot
- pglite/pg for tests? They use real PG. Suggest testcontainers or a CI PG service.
- commitlint/husky hooks

### Libraries suggestions
Frontend:
- @tanstack/react-query (server state)
- @dnd-kit already
- zustand/jotai if client state needed (probably not needed)
- date-fns for reminder dates
- Emoji/nothing
- framer-motion/motion for animations
- sonner (already? they use sonner Toaster? Let me check — they have a Toaster in __root. It's shadcn sonner likely). Yes probably sonner.
- @tanstack/react-virtual for list virtualization if pagination
- PWA: vite-plugin-pwa + Workbox
- Web Push: Push API + service worker
Backend:
- zod (already used for env) — extend to request validation
- express-rate-limit
- helmet
- pino/pino-http for structured logging
- @paralleldrive/cuid2 for ids? They use serial int ids. Maybe switch to cuid for note ids (avoid enumeration). Could suggest UUID/cuid for note ids.
- zod-openapi or @asteasolutions/zod-to-openapi for API docs
- image-size or sharp for dimension parsing
- sharp for image processing (thumbnails)
- node-cron for reminder scheduling
- better-auth plugins: admin, organization (for collaboration), anonymous

Let me verify a few things quickly to be accurate:
- Is there CI? Let me check .github/workflows.
- Is sonner used?
- Monorepo tooling (bun workspaces? turbo?)

Let me do a quick check of root package.json and .github.

Let me verify a few infrastructure details (CI, monorepo tooling, toaster lib) to make recommendations accurate:

**Tool: bash**

**Input:**
```json
{
  "command": "ls -la .github/workflows 2>/dev/null || echo \"no .github/workflows\"; echo \"===ROOT PKG===\"; cat package.json; echo \"===TURBO===\"; ls turbo.json 2>/dev/null || echo \"no turbo.json\""
}
```

**Output:**
```
no .github/workflows
===ROOT PKG===
{
  "name": "google-keep-clone",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run --filter './apps/*' dev",
    "build": "bun run --filter './apps/*' build",
    "test": "bun run --filter backend test && bun run --filter frontend test",
    "test:e2e": "bun run --filter frontend test:e2e",
    "clean": "rm -rf 'apps/*/node_modules' 'packages/*/dist' 'packages/*/.bun'",
    "dev:backend": "bun run --filter backend dev",
    "db:studio": "bun run --filter backend db:studio",
    "dev:frontend": "bun run --filter frontend dev"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
===TURBO===
no turbo.json

```

**Tool: bash**

**Input:**
```json
{
  "command": "grep -E \"sonner|motion|framer|query|virtual|pwa\" apps/frontend/package.json"
}
```

**Output:**
```
    "@tanstack/react-router-ssr-query": "latest",
    "sonner": "^2.0.7",

```

---

## Assistant (Plan · DeepSeek V4 Flash Free (New) · 6.3s)

_Thinking:_

Interesting — there's `@tanstack/react-router-ssr-query` in the frontend package.json. Let me check if TanStack Query is actually available/used, since the explorer said no QueryClient. Let me check.

**Tool: bash**

**Input:**
```json
{
  "command": "grep -rn \"react-query\\|useQuery\\|QueryClient\" apps/frontend/src --include=*.tsx --include=*.ts | head; echo \"===PKG full deps===\"; grep -E '\"@tanstack|\"@base-ui|\"tailwindcss\"|\"motion\"|\"framer\"' apps/frontend/package.json"
}
```

**Output:**
```
zsh:1: no matches found: --include=*.tsx
===PKG full deps===
    "@base-ui/react": "^1.6.0",
    "@tanstack/react-devtools": "latest",
    "@tanstack/react-router": "latest",
    "@tanstack/react-router-devtools": "latest",
    "@tanstack/react-router-ssr-query": "latest",
    "@tanstack/react-start": "latest",
    "@tanstack/router-plugin": "^1.132.0",
    "tailwindcss": "^4.1.18",
    "@tanstack/devtools-vite": "latest",
    "@tanstack/router-cli": "^1.132.0",

```

---

## Assistant (Plan · DeepSeek V4 Flash Free (New) · 4.9s)

**Tool: grep**

**Input:**
```json
{
  "path": "/home/hussein/projects/google-keep-clone/apps/frontend/src",
  "pattern": "useQuery|QueryClient|react-query"
}
```

**Output:**
```
No files found
```

---

## Assistant (Plan · DeepSeek V4 Flash Free (New) · 21.7s)

_Thinking:_

Confirmed: `@tanstack/react-router-ssr-query` is installed but TanStack Query is NOT actually used (`QueryClient`, `useQuery` nowhere). This is a strong signal — the dependency exists but the app uses manual fetch/useEffect. Adding TanStack Query would be a big win and aligns with the already-installed `@tanstack/react-router-ssr-query`.

Now I have enough to write a comprehensive recommendation. Since I'm in plan mode and this is an advisory question, I'll present the recommendations organized by category, prioritized, grounded in file references. Then ask what they want to prioritize/plan next.

Let me structure:

## Priority / quick wins
## 1. Features to add
## 2. Code improvements
## 3. Architecture improvements
## 4. Tools
## 5. Libraries

I should keep it comprehensive but concise. Since the user asked an open list question, I'll give a well-organized answer with prioritization and offer to turn any into a plan. I could use the question tool to ask which area to focus on, but given plan mode, presenting the analysis and asking for direction is appropriate.

Let me write the response. I'll be specific with file references and be honest about impact vs effort.

Here's a comprehensive assessment grounded in the actual codebase. I've grouped by your categories and roughly ordered by impact.

## 1. Features to add

**High-value (Keep parity, fits current architecture):**
- **Reminders + notifications** — per-note reminder timestamps; Web Push (service worker + `vite-plugin-pwa`) + a backend scheduler (`node-cron`) to dispatch due reminders. Big differentiator.
- **Empty trash** — bulk permanent-delete endpoint + "Empty trash" button (currently only per-note permanent delete).
- **Duplicate note** — trivial backend+frontend (copy all fields incl. checklist/labels).
- **Search UX** — highlight matches, search-by-label (backend filter already exists via `filterLabelId`), and a distinct "no results for search" empty state (currently per-view only, `notes-grid.tsx:41–54`).
- **Keyboard shortcuts** — `n` new note, `g` grid/list, `/` focus search, `j/k` navigate.
- **Pagination / infinite scroll** — `getNotes` returns all rows (`notes/service.ts:15-52`); with 100 seeded notes/user this will bite. Cursor-based pagination + `@tanstack/react-virtual`.
- **Export** — Markdown export of a note (copy to clipboard/download).

**Nice-to-have / bigger:**
- **Collaboration/sharing** — better-auth `organization` plugin or a shared-note permission model (multi-user; large change).
- **Note templates**, **Drawings**, **voice notes**, **version history** (Keep parity, larger scopes).
- **PWA/offline** — cache notes locally, queue mutations (natural fit given autosave already exists).

## 2. Code improvements

**Frontend:**
- **Deduplicate `take-note-input.tsx` (544 lines) and `note-form.tsx` (458)** — identical `NOTE_COLORS`, Zod schema, and color/label/image/checklist handlers (~200 dup lines). Extract shared `useNoteEditor` hook + shared sub-components.
- **Deduplicate the checklist progress bar** (3 copies: `note-card.tsx:129-150`, `checklist-editor.tsx:102-116`, unused `ChecklistProgress` at :121-138) into one component.
- **Error handling** — page loaders swallow errors (`notes-page.tsx:43-44` `catch { setNotes([]) }`); add error states, toasts via the existing sonner `Toaster`, and axios response interceptors for consistent error messages.
- **Skeletons instead of spinners** — `skeleton.tsx` exists but is unused by the notes grid.
- **Accessibility** — `note-card.tsx:48-62` `role="button"` div handles Enter but not Space; icon buttons need `aria-label` (only `title`); the hand-rolled delete overlay in `edit-note-dialog.tsx:98-133` lacks focus trap/`aria-labelledby` — replace with the existing Base UI `AlertDialog` primitive.
- **Optimistic UI** for pin/archive/trash (only reorder is optimistic today).
- **Layer inversion** — `checklist-utils.ts:1` imports `createChecklistItem` from a component; move that helper into the utils file.

**Backend:**
- **Global error middleware** — no `ErrorRequestHandler` in `app.ts`; unhandled errors hit Express's default HTML handler (not JSON).
- **Stop leaking errors** — `notes/controller.ts:35` does `res.status(500).json({ error })` exposing the raw error/stack.
- **Request validation** — Zod is used only for env; controllers hand-check fields. Add Zod schemas per endpoint (and share them with the frontend).
- **Indexes** — no `user_id` index on `notes` (FK cascade delete scans); add for `notes`, `note_labels`.
- **Transactions** — `softDeleteNote`/`restoreNote`/`permanentDeleteNote` aren't transactional (`notes/service.ts`).
- **S3 orphan bug** — `uploadNoteImage` uploads to S3 *before* the DB insert (`notes/controller.ts:162-164`); a DB failure orphans the object.
- **Remove `(req as any).user.id`** (~13 spots) via Express `Request` type augmentation for `user`/`session`.
- **Dedupe** — presigned-URL mapping duplicated 3× (`notes/service.ts:63-82,135-154,326-351`); `setNoteLabelsTx` duplicates `setNoteLabels`.

## 3. Architecture improvements

- **Adopt TanStack Query** — the biggest win. `@tanstack/react-router-ssr-query` is already installed but unused (no `QueryClient` anywhere); server state is manual `useState`+`useEffect` with fetch-after-every-mutation. This fixes caching, deduping, background refetch, and gives you optimistic updates + loading state for free.
- **Shared types/schemas package** — a `packages/shared` in the monorepo (bun workspaces already set up) for Zod schemas + inferred types used by both apps; kills the drift between `NoteChecklistItem` definitions.
- **Typed API client from OpenAPI** — generate FE types from an OpenAPI spec (`zod-openapi` + `openapi-typescript`) so the axios wrappers become type-checked end-to-end.
- **Session caching** — `requireAuth` calls `getSession` on every request (`middleware.ts:4-18`); add better-auth session cache or a short TTL.
- **Error taxonomy** — one consistent `{ error: { code, message, details } }` shape app-wide.
- **Scheduler/notification service** — for reminders, a separate concern (cron dispatcher + push/email channel) rather than mixing into notes.

## 4. Tools

- **CI/CD (biggest gap)** — no `.github/workflows` at all. Add GitHub Actions: `bun install && bun run check && bun run test` on PR, plus Playwright e2e and backend tests against a Postgres service container.
- **Turborepo** for the monorepo (task caching; today it's plain bun workspaces + `--filter` scripts).
- **Sentry** for client+server error monitoring (pairs with the error-handling work above).
- **Dependabot/Renovate** for dependency updates (several `"latest"` pins are risky).
- **Husky + lint-staged** pre-commit hooks to run Biome.
- **Postgres testcontainers** (or a CI service) instead of relying on a local PG for backend tests.
- **Vitest coverage reporting** — thresholds exist in `vitest.config.ts` but there's no coverage report/CI enforcement.

## 5. Libraries

**Frontend:**
- `@tanstack/react-query` — server state (see above; dep already installed).
- `vite-plugin-pwa` + Workbox — PWA/offline + Web Push.
- `@tanstack/react-virtual` — virtualize the notes list once pagination lands.
- `date-fns` — reminder/date formatting.
- `motion` (framer-motion) — tasteful card/modal animations.

**Backend:**
- `helmet` — security headers (currently none).
- `express-rate-limit` — rate-limit auth + note endpoints (no protection today).
- `pino` + `pino-http` — structured logs with request IDs (only `morgan("dev")` now).
- `sharp` + `image-size` — parse image dimensions (currently always `null`) and generate thumbnails.
- `zod-openapi` — request validation + OpenAPI generation in one step.
- `node-cron` — reminder scheduler.
- better-auth plugins — `organization` (collab), `admin`, or `anonymous` as features grow.
- Consider `cuid2`/UUID for `notes.id` — currently `serial` ints are enumerable/guessable.

---

Want me to turn any of these into a concrete plan? My top recommendations to start with: **(1) TanStack Query adoption**, **(2) backend global error middleware + Zod validation + CI**, and **(3) a couple of quick feature wins** (empty trash, duplicate note, search empty-state). I can also put together a prioritized roadmap across all of them if you prefer.

---

